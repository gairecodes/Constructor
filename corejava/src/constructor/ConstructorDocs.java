package constructor;

public class ConstructorDocs {
	/*
	 * constructor >> kei banaune /kei banauxa ie used to create object ie constructor le object banauxa
	 * special type of method which has same name as class name ie yesle object banauxa
	 * if theres is no any user defined constructor in class then java provides default constructor thats why
	 * 
	 *  
	 *  remember constructor returns object because it creates one ie kam vanekai banaune ho so return pani garxa
	 * 
	 * 
	 * types of constructor
	 * 
	 * 1.default (used to create objects and used to sset default values )
	 * 
	 * class_name(){
	 *set default values to object
	 * }
	 * 
	 * 
	 * 
	 * 2.parameterized 
	 * used to create objects and set dynamic values to it 
	 * 
	 * 
	 * syntax:
	 * 
	 * class_name(arg1,arg2....argn){
	 * set dynamic object to object
	 * }
	 * 
	 * 
	 */
}
